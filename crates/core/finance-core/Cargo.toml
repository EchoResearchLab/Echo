[package]
name = "echo-finance-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deterministic decimal finance kernel for Echo Research"

[dependencies]
rust_decimal = { version = "1", default-features = false }
